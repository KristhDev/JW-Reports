import { AddOrEditRevisit } from '@revisits/screens';

/**
 * This screen is responsible for grouping the components to add or
 * edit a revisit.
 *
 * @return {JSX.Element} rendered component to show form to add or edit a revisit
 */
export default function AddOrEditRevisitScreen(): JSX.Element {
    return (<AddOrEditRevisit />);
}