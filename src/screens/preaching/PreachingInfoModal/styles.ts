import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    modal: {
        borderRadius: 10,
        padding: 24
    },

    modalTitle: {
        fontSize: 18,
        fontWeight: 'bold',
        marginBottom: 24
    },

    modalSection: {
        marginBottom: 16
    },

    modalSectionText: {
        fontSize: 17,
    }
});

export default styles;