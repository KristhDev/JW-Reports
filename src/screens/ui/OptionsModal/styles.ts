import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    modalTitle: {
        fontSize: 20,
        fontWeight: 'bold',
        textAlign: 'left'
    },

    modalTitleContainer: {
        marginBottom: 10,
        marginLeft: 5,
        padding: 0
    }
});

export default styles;