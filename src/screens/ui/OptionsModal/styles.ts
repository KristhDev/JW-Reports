import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    modalTitle: {
        fontSize: 20,
        fontWeight: 'bold',
        textAlign: 'left'
    },

    modalTitleContainer: {
        marginBottom: 24,
        // marginLeft: 5,
        padding: 0
    }
});

export default styles;