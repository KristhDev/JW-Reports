import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    sectionStyle: {
        marginBottom: 32,
        width: '100%'
    },

    sectionSubTitle: {
        fontSize: 19,
        fontWeight: 'bold',
        marginBottom: 16
    },

    imageText: {
        fontSize: 16,
        marginTop: 10
    },

    dateCreatedText: {
        bottom: 24,
        fontSize: 16,
        position: 'absolute',
        right: 24,
    }
});

export default styles;