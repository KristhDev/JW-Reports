import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        alignItems: 'center',
        flexDirection: 'row',
        marginBottom: 40,
        width: '100%',
    },

    text: {
        fontSize: 18,
        paddingLeft: 8,
        textAlign: 'left',
    }
});

export default styles;