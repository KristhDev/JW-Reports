import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        padding: 10
    },

    text: {
        fontSize: 17,
        textAlign: 'center'
    }
});

export default styles;