import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        padding: 16
    },

    text: {
        fontSize: 17,
        textAlign: 'center'
    }
});

export default styles;