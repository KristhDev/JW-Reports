import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    radioContainer: {
        alignItems: 'center',
        flexDirection: 'row',
        padding: 5
    },

    radioLabel: {
        fontSize: 18,
        marginLeft: 15
    }
});

export default styles;