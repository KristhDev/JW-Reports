import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    radioContainer: {
        alignItems: 'center',
        flexDirection: 'row',
        marginBottom: 5
    },

    radioLabel: {
        fontSize: 18,
        marginLeft: 8
    }
});

export default styles;