import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    sectionContainer: {
        borderBottomWidth: 1,
        paddingTop: 15,
    },

    sectionTitle: {
        fontSize: 15,
        fontWeight: 'bold',
        textAlign: 'left'
    }
});

export default styles;