import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    focusExternalBorder: {
        borderRadius: 7,
        borderWidth: 1
    },

    focusInternalBorder: {
        borderWidth: 1.5,
        paddingRight: 10
    },

    defaultBorder: {
        borderRadius: 5,
        borderWidth: 0.5
    }
});

export default styles;