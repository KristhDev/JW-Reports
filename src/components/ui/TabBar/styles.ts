import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        alignItems: 'center',
        flexDirection: 'row',
        height: 60,
        overflow: 'hidden',
        width: '100%'
    }
});

export default styles;