import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    table: {
        alignItems: 'center',
        borderWidth: 1
    },

    tableRow: {
        alignItems: 'center',
        flexDirection: 'row',
        width: '100%'
    }
});

export default styles