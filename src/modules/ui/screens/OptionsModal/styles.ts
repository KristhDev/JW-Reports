import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    modalTitle: {
        fontSize: 20,
        fontWeight: 'bold',
        textAlign: 'left'
    },

    modalTitleContainer: {
        marginBottom: 24,
        padding: 0
    }
});

export default styles;